jdk: openjdk17

build:
  before_install:
    - echo "Before setting JAVA_HOME"
#    - java -version
#    - export JAVA_HOME=/home/jitpack/tools/jdk17
#    - export PATH=$JAVA_HOME/bin:$PATH
#    - echo "After setting JAVA_HOME"
    - java -version

  install:
    - ./gradlew :sdk:assembleRelease --no-daemon