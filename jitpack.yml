jdk: openjdk17

build:
  # 先让它构建 :sdk:assembleRelease
  before_install:
    - ./gradlew :sdk:assembleRelease
  # 如果使用 maven-publish，需要执行 publishToMavenLocal/publish
  after_success:
    - ./gradlew :sdk:publishToMavenLocal